name = "doclix"
compatibility_date = "2024-09-01"
pages_build_output_dir = "dist"

# Secrets (set via `wrangler pages secret put <NAME>` or the Cloudflare
# dashboard — never committed here):
#   SUPABASE_URL
#   SUPABASE_ANON_KEY
#
# At build time these are exposed to Vite as VITE_SUPABASE_URL and
# VITE_SUPABASE_ANON_KEY (see package.json build script / CI config).
